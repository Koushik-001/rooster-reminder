name: Development Build

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android Development Client
    type: build
    params:
      platform: android
      profile: development
